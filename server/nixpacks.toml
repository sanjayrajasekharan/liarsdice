[phases.setup]
nixPkgs = ["nodejs_20"]
cmds = ["corepack enable"]

[phases.install]
cmds = ["cd .. && pnpm install"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "node dist/server/index.js"
